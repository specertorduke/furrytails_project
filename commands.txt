use this after cloning (make sure you have a database first named furrytails_project & this repo folder is in HTDOCS of xampp): 
    php artisan migrate
    php artisan db:seed

php artisan storage:link

npm run dev
    ctrl c to stop

use this for re-setting db:
    php artisan migrate:fresh --seed

-----------------------------------------------------
@csrf - security for forms

php artisan cache:clear
php artisan config:clear

php artisan optimize:clear
php artisan route:clear
php artisan view:clear

php artisan migrate:rollback

<?php
// In any blade view:
<div>Appointment: {{ $appointment->time_period }}</div>
<div>Starts: {{ $appointment->time_until }}</div>

<?php
// In any blade view:
<x-appointment-status-badge :status="$appointment->status" />

for both tabs
completed is gray, confirmed is green
pending is yellow, active is orange